import { Typography, Box } from '@mui/material';

export default function Transactions() {
  return (
    <Box>
      <Typography variant="h4">Transactions</Typography>
      <Typography sx={{ mt: 2 }}>Transactions page coming soon...</Typography>
    </Box>
  );
}