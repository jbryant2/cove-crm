import { Typography, Box } from '@mui/material';

export default function Contacts() {
  return (
    <Box>
      <Typography variant="h4">Contacts</Typography>
      <Typography sx={{ mt: 2 }}>Contacts page coming soon...</Typography>
    </Box>
  );
}